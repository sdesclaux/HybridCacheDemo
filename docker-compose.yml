version: '3.9'
services:
    redis:
        image: redis:latest
        restart: always
        ports:
            - "6379:6379"
        volumes:
            - redis:/data
        profiles: ["local"]
volumes:
  redis:
